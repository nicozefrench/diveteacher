# ═══════════════════════════════════════════════════════
# DiveTeacher - PRODUCTION Environment Template
# ═══════════════════════════════════════════════════════
# 
# INSTRUCTIONS:
# 1. Copy this file to .env on your production server
# 2. Fill in all REPLACE_ME values with real secrets
# 3. chmod 600 .env (protect the file)
# 4. NEVER commit the real .env to git
#
# ═══════════════════════════════════════════════════════

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false  # ← PRODUCTION: false
CORS_ORIGINS=https://diveteacher.io,https://diveteacher.app

# LLM Configuration
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=qwen2.5:7b-instruct-q8_0  # ← GPU version (Q8_0 for production)

# RAG Configuration
RAG_TOP_K=5
RAG_TEMPERATURE=0.7
RAG_MAX_TOKENS=2000
RAG_STREAM=true
RAG_MAX_CONTEXT_LENGTH=4000

# Qwen-Specific Configuration
QWEN_TEMPERATURE=0.7
QWEN_TOP_P=0.9
QWEN_TOP_K=40
QWEN_NUM_CTX=4096

# Neo4j Database (Production)
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=REPLACE_WITH_STRONG_PRODUCTION_PASSWORD  # ← CHANGE THIS!
NEO4J_DATABASE=neo4j

# Graphiti Knowledge Graph
GRAPHITI_ENABLED=true

# File Upload
UPLOAD_DIR=/uploads
MAX_UPLOAD_SIZE_MB=200
ALLOWED_EXTENSIONS=pdf,ppt,pptx,doc,docx

# Processing
DOCLING_TIMEOUT=300
PROCESSING_WORKERS=4  # ← More workers on production GPU server

# Sentry (Error Tracking - Production)
SENTRY_DSN_BACKEND=REPLACE_ME  # ← Get from Sentry dashboard
VITE_SENTRY_DSN=REPLACE_ME
SENTRY_ENVIRONMENT=production  # ← PRODUCTION
SENTRY_TRACES_SAMPLE_RATE=0.1  # ← Lower sampling for production (cost)

# Supabase (Auth Multi-Users - Production)
SUPABASE_URL=REPLACE_ME  # ← Get from Supabase dashboard
SUPABASE_ANON_KEY=REPLACE_ME
SUPABASE_SERVICE_KEY=REPLACE_ME

# Frontend (Production URLs)
VITE_API_URL=https://api.diveteacher.io  # ← Your production API domain
VITE_SUPABASE_URL=REPLACE_ME
VITE_SUPABASE_ANON_KEY=REPLACE_ME
FRONTEND_URL=https://diveteacher.io

# OpenAI API (for embeddings)
OPENAI_API_KEY=REPLACE_ME  # ← Use production API key (separate from dev)

# Anthropic API (Graphiti Claude Haiku 4.5)
ANTHROPIC_API_KEY=REPLACE_ME  # ← Use production API key (separate from dev)

